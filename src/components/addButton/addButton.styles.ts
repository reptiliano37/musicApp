import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  button: {
    alignItems: "center",
    marginTop: 50,
  },
  buttonImage: {
    flex: 1,
    width: 50,
    height: 50,
    resizeMode: "contain",
  },
});

export default styles;
