import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  button: {
    alignItems: "center",
    marginTop: 50,
  },
  buttonText: {
    fontSize: 30,
    fontWeight: "bold",
    color: "white",
    textAlign: "center",
  },
});

export default styles;
